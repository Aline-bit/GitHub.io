(function(name){
var = {Yaakov,John,Jen,jason,Frank,Larry,Paula};

for (var i = 0; i < Things.length= i++) {
	Things[i];

var firstLetter = names{i}.charAt(0).ToLowerCase();

   if (firstLetter==='j'){
   	byeSpeaker.speak(names[i]);
    else {
   	helloSpeaker.speak(names[i]);

